---
import BaseLayout from '../layouts/BaseLayout.astro';
import Nav from '../components/Nav.astro';
import StructureField from '../components/StructureField.astro';
import { pathFor } from '../lib/site';
---

<BaseLayout title="Journal — Arcane Orion">
  <StructureField mode="triad" />
  <div class="relative z-10">
    <Nav />

    <main class="page-shell py-16">
      <header class="grid gap-8 py-10 lg:grid-cols-[0.9fr_1.1fr] lg:items-end">
        <div>
          <p class="axis-label" style="color: #b08940">drift · note</p>
          <h1 class="mt-4 font-display text-5xl font-bold leading-none text-arcane-text md:text-7xl">
            Journal
          </h1>
          <p class="mt-6 max-w-2xl text-lg leading-8 text-arcane-text-secondary">
            非主线的杂记 · 临时观测 · 短想法
          </p>
        </div>

        <div class="observatory-panel flex items-center justify-between p-6">
          <div>
            <p class="font-mono text-[0.62rem] uppercase tracking-[0.32em] text-arcane-text-dim">satellite glyph</p>
            <p class="mt-3 font-display text-5xl" style="color: #b08940">∿</p>
          </div>
          <a href={pathFor('/observatory')} class="link-quiet font-mono text-[0.62rem] uppercase tracking-[0.32em]">
            ← observatory
          </a>
        </div>
      </header>

      <section class="mt-8 observatory-panel p-12 text-center">
        <p class="font-display text-3xl text-arcane-text-dim">日记轨道刚被打开。</p>
        <p
          class="mt-6 mx-auto max-w-xl text-base leading-8 text-arcane-text-secondary"
          style="font-family: var(--font-italic); font-style: italic;"
        >
          这里不为主题服务。<br />
          只为漂浮的想法、未成形的提问、和某些短暂的状态留一个停靠点。
        </p>
        <p class="mt-8 font-mono text-[0.62rem] uppercase tracking-[0.32em] text-arcane-text-dim">awaiting first drift</p>
      </section>
    </main>

    <footer class="border-t border-arcane-border py-10">
      <div class="page-shell text-center font-mono text-[0.72rem] uppercase text-arcane-text-dim">
        arcane.orion © 2026
      </div>
    </footer>
  </div>
</BaseLayout>
